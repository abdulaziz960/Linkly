.DS_Store
.next/
.npm-cache/
node_modules/
npm-debug.log*
.env
.env.local
data/*.sqlite
data/*.db
# CI's throwaway build database (.github/workflows/ci.yml) and every test
# file's isolated tests/.tmp-*.db (created/removed per-file, but left
# behind uncommitted if a test crashes mid-run instead of hitting its
# afterAll cleanup).
*.db
*.db-journal
*.db-wal
*.db-shm
prisma/.generated/
app/generated/
preview_pdf/
*.php
.env*
!.env.example
tsconfig.tsbuildinfo
playwright-report/
test-results/
SAAS_AUDIT_REPORT.md
